import React from 'react';
import OnboardingFlow from './OnboardingFlow';

const IAOnboardingFlow = (props) => {
  return <OnboardingFlow {...props} serviceType="IA" />;
};

export default IAOnboardingFlow;